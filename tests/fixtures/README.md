# fixtures/ — what each dataset is (one line per file, added with the file)

<!-- `<file>` — source, format, which tests read it. Example:
`listing_page.html` — saved source page (2026-07-12) for `unit/test_adapter.py` (no live requests in CI);
`gold_labels.jsonl` — 300-item gold set for the /keel-pilot validate gate. -->
