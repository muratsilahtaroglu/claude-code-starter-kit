# e2e/ — why each smoke test exists (one line per file, added with the file)

<!-- `test_<name>.py` — which user-visible flow it walks end-to-end + origin. Example:
`test_search_smoke.py` — start API → query → ranked results come back (phase 4 gate). -->
