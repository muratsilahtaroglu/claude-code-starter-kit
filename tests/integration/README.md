# integration/ — why each test exists (one line per file, added with the file)

<!-- `test_<name>.py` — what it guards + which real dependency it exercises + origin. Example:
`test_repo_roundtrip.py` — save→load through real Postgres keeps types intact (phase 3). -->
